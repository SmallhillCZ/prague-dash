import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cz.smallhill.praguedash',
  appName: 'PragueDash',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
